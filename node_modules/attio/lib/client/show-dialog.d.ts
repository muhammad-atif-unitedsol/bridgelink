import React from "react";
export type Dialog = (props: {
    hideDialog: () => void;
}) => React.JSX.Element;
interface DialogOptions {
    /**
     * The title of the toast.
     */
    title: string;
    /**
     * The contents of the dialog.
     */
    Dialog: Dialog;
}
/**
 * Opens a dialog.
 *
 * @example
 *
 * import {showDialog} from "attio/client";
 *
 * await showDialog({
 *     title: "Invoice sent",
 *     Dialog: ({hideDialog}) => <MyDialogComponent hideDialog={hideDialog} />,
 * });
 * ```
 * @returns A promise that resolves when the dialog was successfully opened and closed.
 * Rejects if the dialog could not be opened. This could happen if the user is prompted with
 * unsaved changes and cancels the opening of this dialog.
 */
export declare function showDialog(options: DialogOptions): Promise<void>;
export {};
//# sourceMappingURL=show-dialog.d.ts.map