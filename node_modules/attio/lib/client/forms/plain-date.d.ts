import type { FormValue } from "./value";
export interface FormPlainDate<TOptional extends boolean = false> extends FormValue {
    type: "plain-date";
    /**
     * Whether this plain date is optional, i.e. allows `undefined` values.
     */
    isOptional: TOptional;
    /**
     * Make this plain date optional, i.e. allow `undefined` values.
     */
    optional(): FormPlainDate<true>;
    /**
     * Set the default value of this plain date. The default value will be returned if the user
     * doesn't provide a value.
     *
     * NOTE that this is not the same as an initial value!
     */
    default(value: string): FormPlainDate<TOptional>;
}
/**
 * Create a new plain date form value.
 */
export declare function plainDate(): FormPlainDate<false>;
//# sourceMappingURL=plain-date.d.ts.map