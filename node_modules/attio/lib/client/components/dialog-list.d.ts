import type React from "react";
import type { Icon } from "../icon";
declare const DialogListItem: (props: {
    /**
     * The children will be rendered as the text of the list item.
     */
    children: React.ReactNode;
    /**
     * The icon of the list item.
     */
    icon: Icon;
    /**
     * The action label of the list item.
     */
    actionLabel: string;
    /**
     * The on trigger handler of the list item.
     */
    onTrigger: () => void | Promise<void>;
    /**
     * The suffix that will be rendered on the right side of the list item.
     */
    suffix?: React.ReactNode;
}) => React.JSX.Element;
export declare const DialogList: {
    (props: {
        /**
         * A message and action buttons to display when the list is empty.
         */
        emptyState: {
            /**
             * The empty state's text.
             */
            text: string;
            /**
             * The empty state's actions.
             */
            actions?: Array<{
                /**
                 * The text appearing on the button.
                 */
                text: string;
                /**
                 * A function to run when the button is triggered.
                 */
                onTrigger: () => void;
                /**
                 * An optional button icon.
                 */
                icon?: Icon;
            }>;
        } | string;
        children: React.ReactNode;
    }): React.JSX.Element;
    Item: typeof DialogListItem;
};
export {};
//# sourceMappingURL=dialog-list.d.ts.map