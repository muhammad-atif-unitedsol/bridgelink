import type { Override } from "../../../utils";
import type { BaseNode, Optional } from "../../schema-utils";
export interface WorkflowOutcomeDataAttioList<TOptions extends {
    list_slug: string;
} | {} = {}> extends BaseNode<"attio-list"> {
    optional(): Optional<this>;
    list(args: {
        slug: string;
    }): WorkflowOutcomeDataAttioList<Override<TOptions, {
        list_slug: string;
    }>>;
}
//# sourceMappingURL=attio-list.d.ts.map