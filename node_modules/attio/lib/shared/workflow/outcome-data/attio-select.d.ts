import type { Override } from "../../../utils";
import type { BaseNode, Optional } from "../../schema-utils";
export interface WorkflowOutcomeDataAttioSelect<TOptions extends {
    object_slug: string;
    attribute_slug: string;
} | {} = {}> extends BaseNode<"attio-select"> {
    optional(): Optional<this>;
    attribute(args: {
        objectSlug: string;
        attributeSlug: string;
    }): WorkflowOutcomeDataAttioSelect<Override<TOptions, {
        object_slug: string;
        attribute_slug: string;
    }>>;
}
//# sourceMappingURL=attio-select.d.ts.map