import type { ArrayNode } from "./array-node";
import type { BaseNode } from "./base-node";
import type { Friendly } from "./friendly";
import type { StructNode } from "./struct-node";
export type NormalizedObject<TObject> = {
    [K in keyof TObject]: TObject[K] extends Friendly<BaseNode> ? Normalized<TObject[K]> : never;
};
/**
 * Recursively transforms all plain objects inside a friendly node to structs.
 */
export type Normalized<TFriendlyNode extends Friendly<any>> = TFriendlyNode extends StructNode<infer TObject extends {
    [key: string]: Friendly<BaseNode>;
}> ? StructNode<NormalizedObject<TObject>> : TFriendlyNode extends ArrayNode<infer TElement extends Friendly<BaseNode>> ? ArrayNode<Normalized<TElement>> : TFriendlyNode extends Exclude<BaseNode, StructNode<any> | ArrayNode<any>> ? TFriendlyNode : StructNode<NormalizedObject<TFriendlyNode>>;
//# sourceMappingURL=normalize.d.ts.map