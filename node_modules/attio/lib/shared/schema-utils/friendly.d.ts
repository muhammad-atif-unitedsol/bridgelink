import type { ArrayNode } from "./array-node";
import type { StructNode } from "./struct-node";
/**
 * Friendly nodes support plain objects in place of structs.
 * You can transform them back into a strict node using `normalize`.
 */
export type Friendly<TNode> = TNode | ArrayNode<Friendly<TNode>> | StructNode<{
    [key: string]: Friendly<TNode>;
}> | {
    [key: string]: Friendly<TNode>;
};
//# sourceMappingURL=friendly.d.ts.map