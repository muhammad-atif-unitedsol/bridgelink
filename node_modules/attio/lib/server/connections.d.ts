interface BaseConnection {
    /** The unique identifier for the connection. */
    id: string;
    /** The access token or secret for the connection. */
    value: string;
    createdBy: {
        type: "user";
        /** User ID */
        id: string;
    };
}
export interface UserConnection extends BaseConnection {
    ownedBy: {
        type: "user";
        /** User ID */
        id: string;
    };
}
export interface WorkspaceConnection extends BaseConnection {
    ownedBy: {
        type: "workspace";
        /** Workspace ID */
        id: string;
    };
}
export type Connection = UserConnection | WorkspaceConnection;
export declare function getUserConnection(): UserConnection;
export declare function getWorkspaceConnection(): WorkspaceConnection;
export {};
//# sourceMappingURL=connections.d.ts.map