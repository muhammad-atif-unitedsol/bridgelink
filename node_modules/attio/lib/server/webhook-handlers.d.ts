export interface WebhookHandler {
    /** The unique identifier for the webhook handler. */
    id: string;
    /** The URL to trigger webhook handler. */
    url: string;
    /**
     * The file that will be executed when the webhook handler is triggered.
     * The file must be located in the `webhooks` directory and have .webhook.ts extension.
     *
     * Example: `webhooks/[fileName].webhook.ts`
     */
    fileName: string;
    /**
     * The optional identifier of the associated webhook in the external system.
     */
    externalWebhookId: string | null;
}
export declare function createWebhookHandler({ fileName, }: {
    fileName: string;
}): Promise<WebhookHandler>;
export declare function listWebhookHandlers(): Promise<Array<WebhookHandler>>;
export declare function getWebhookHandler(id: string): Promise<WebhookHandler>;
export declare function updateWebhookHandler(id: string, { externalWebhookId }: {
    externalWebhookId: string;
}): Promise<void>;
export declare function deleteWebhookHandler(id: string): Promise<void>;
//# sourceMappingURL=webhook-handlers.d.ts.map