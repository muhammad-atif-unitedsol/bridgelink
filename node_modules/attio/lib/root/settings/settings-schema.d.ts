import type { SettingsNode } from "../../shared";
export type ScopedSettingsSchema = Record<string, SettingsNode>;
/**
 * Internal settings schema that includes both user and workspace settings.
 * This is used internally by the SDK but not exposed to developers.
 *
 * Do not use this type directly. Use the SettingsSchema instead.
 *
 * @deprecated Use SettingsSchema instead.
 */
export interface InternalSettingsSchema {
    user: ScopedSettingsSchema;
    workspace: ScopedSettingsSchema;
}
export interface SettingsSchema {
    workspace: ScopedSettingsSchema;
}
//# sourceMappingURL=settings-schema.d.ts.map