declare class Blocker {
    private stream;
    private interface?;
    private blocked;
    constructor(stream?: NodeJS.ReadStream);
    onKeypress: (_: string, key: {
        ctrl?: boolean;
        name?: string;
    }) => void;
    isBlocked: () => boolean;
    block: () => void;
    unblock: () => void;
    toggle: (force?: boolean) => void;
}
export default Blocker;
